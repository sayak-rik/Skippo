import { ComingSoonPage } from "../../../components/ComingSoonPage";

export default function RolesPage() {
  return (
    <ComingSoonPage
      icon="🔐"
      title="Roles & Permissions"
      badge="Administration"
      description="Define custom roles with granular permissions across Students, Staff, Finance, Attendance, Exams, and Settings. Assign roles to admin users."
    />
  );
}
