SKIPPO — COMPLETE FEATURE LIST
================================
Last updated: 2026-05-25
Source: features.txt, README.md, Skippo_Development_Document.docx

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PLATFORM OVERVIEW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Five surfaces under one platform:
  - Parent App       (React Native — iOS + Android)
  - Driver/Conductor App  (React Native — Android)
  - Teacher App      (Expo — React Native Web)
  - Admin Dashboard  (Next.js Web)
  - Backend API      (Django + DRF + Daphne ASGI)

Multi-tenant architecture:
  - Shared database with school-scoped data (school_id / slug on every record)
  - Each school has a unique slug used in URLs and token scoping
  - Superuser sees all; school admin sees only their school

Authentication:
  - OTP-based login for parents and drivers (phone number)
  - JWT access + refresh tokens for all user types
  - Email/password for admin dashboard users
  - Invite-token signup flow for teachers and drivers
  - FCM token update endpoint for push notification registration

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARENT APP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Authentication
  - Phone + OTP login (6-digit code)
  - New parent signup (3-step): OTP → child name/grade → bus route selection
  - Supports multiple children per account

Home Screen
  - Live ETA badge showing how many stops away the bus is
  - Trip status pill (Scheduled / Active / Completed)
  - Bus label / route name

Live Tracking (Track Tab)
  - Animated bus marker on Google Maps updating every 60 seconds via WebSocket
  - LIVE badge shown when trip is active
  - Student stops displayed on map
  - Call driver button on driver contact card
  - Auto-reconnect on network drop (WebSocket with exponential backoff)

Driver / Conductor Contact
  - View conductor profile: name, photo, verification badge
  - Call driver at any time, even when no trip is active

Route & Stop Management
  - Change bus route from Profile tab (pick from available routes)
  - Edit pickup/drop stop — custom stop name saved, shown to conductor with badge

Notifications Tab
  - Live alerts feed: boarding confirmations, drop confirmations
  - Breakdown warnings
  - Missed-drop alerts
  - SOS / panic events (critical priority)
  - SMS fallback for parents without internet (MSG91)

Attendance History
  - Full history of all trips and boarding/dropping records per child

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DRIVER / CONDUCTOR APP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Authentication & Onboarding
  - Phone + OTP login for existing drivers
  - Invite-code signup: enter token → school auto-fills → complete profile → immediate access
  - Self-signup (pending approval): select school → fill details → wait for admin approval
  - "Check approval status" to simulate/trigger approval

Face Verification
  - Selfie match against stored photo before trip start
  - Rejects wrong faces; accepts conductor in varied lighting conditions

Dashboard Tab
  - Start trip button → status changes to ACTIVE, GPS ping indicator turns green
  - Floating SOS button appears on all tabs when trip is active
  - End trip → saves trip record
  - Switch vehicle if multiple vehicles are assigned

Roster Tab
  - Full student list for the active route
  - Tap Board / Drop to update student status (ABSENT → BOARDED → DROPPED)
  - One-thumb interaction design: large tap targets
  - Students with parent-set custom stops show a "Parent set a custom stop" badge
  - Offline-first: queues board/drop actions, syncs on reconnect

SOS / Emergency
  - SOS tab with large panic button
  - Trigger SOS → full-screen red overlay with step-by-step instructions
  - All parents on the route receive a critical alert instantly
  - School admin receives SOS notification
  - SMS bulk alert sent to all parent phones as fallback
  - SOS notification reaches school admin within ~10 seconds target latency

Breakdown Screen
  - "Alert all parents now" → parents receive warning-level alert
  - Confirmation card shown after alert is sent
  - Nearby school vehicles shown with Call buttons

Floating SOS Button
  - Persists across all tabs when a trip is active
  - One-tap emergency trigger without navigating away

Devices Tab
  - Lists all devices linked to the conductor's Aadhaar
  - Multiple devices supported (configurable OTP limit)

Renewals Tab
  - Upcoming document renewals: fitness certificate, insurance, etc.
  - Urgency level indicators (urgent / soon / ok)

Background GPS (Critical Feature)
  - Uses expo-location foreground service to prevent Android from killing location
  - Sends GPS coordinates every 10 seconds or every 20 meters (whichever comes first)
  - WebSocket-based real-time location broadcast to all parents on the route
  - Foreground notification keeps service alive: "Skippo Trip Active"
  - GPS usage target: under 15% battery per 1-hour trip

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEACHER APP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Authentication & Onboarding
  - Phone + OTP login for existing teachers
  - Invite-code signup: enter token → school/email auto-fills → complete profile
  - First-week class-picker setup: select classrooms and configure Mon–Fri availability
  - Setup stored as schedule preferences; auto-generates ClassSession records

Schedule Tab
  - Today's sessions listed and highlighted
  - Current session highlighted
  - Tap a session to open the attendance roster for that class

Attendance Roster
  - Mark students present or absent
  - Add a per-student comment/note
  - Save attendance for the session

Broadcast Tab
  - New broadcast: compose message, select target classrooms, send
  - Broadcast history with delivery timestamps
  - Parents receive push notifications for broadcasts

Assist Requests Tab
  - Lists pending parent questions per student
  - Reply modal: type response → mark as resolved
  - Resolved requests shown with green checkmark

AI Teaching Assistant
  - Token status tracker (daily Gemini token limit per teacher)
  - Lesson plan generator
  - Class summary generator
  - Voice observation: speak a note about a student → transcribed and stored

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — TRANSPORT & FLEET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Fleet Overview (Live Map)
  - All active buses shown on one Google Map with live markers
  - Bus status cards alongside map: route name, conductor, students boarded/total, status
  - WebSocket real-time updates (socket.io-client connection)

Driver Management
  - List and manage all drivers/conductors
  - View KYC status, verification badge, documents
  - Approve or reject self-signup requests
  - Invite new drivers via token

Live Fleet Tracking
  - Live GPS positions for all active trips
  - Per-trip status: scheduled / active / completed

Car Pickup / Dismissal
  - Manage dismissal queue
  - WebSocket-based real-time dismissal state

Route Management
  - Create, edit, delete routes
  - Map-based route and stop creation
  - Assign buses and conductors to routes
  - Stop ordering (sequence management)
  - Stop GPS coordinates stored

Call Management
  - Call log and management

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — ACADEMICS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MODULE A — SCHOOL SETUP

Academic Years & Sessions
  - Create, edit, delete academic years with start/end dates
  - Mark one year as "current"
  - Add multiple sessions within a year (Term 1, Term 2, etc.)

Subjects
  - Create, edit, delete subjects with optional subject code
  - Activate / deactivate subjects
  - Color-coded subject cards in the dashboard

Houses / Groups
  - Create school houses with custom colors (8 presets + color picker)
  - Edit and delete houses
  - Assign students to houses during admission

Academic Calendar
  - Holidays: national, regional, and school holidays with year filter
  - Academic Events: exam, sports, cultural, parent meeting, other — with date range and description

Role-Based Access Control (RBAC)
  - Define custom roles with granular permission checkboxes
  - 15 permission codes across 6 groups (Students, Staff, Finance, Attendance, Exams, Settings)
  - Assign/revoke roles per user
  - Superuser wildcard ["*"] sees everything
  - Permission-aware sidebar: nav items hidden based on user's role

──────────────────────────────────────────────────────────
MODULE B — STUDENT MANAGEMENT

Student Admission
  - Full admission form: personal info, academic placement, contact/address, parent/guardian
  - Auto-generated admission number: {SLUG}/{YEAR}/{SEQ:0004d}
  - Assign classroom, roll number, house at admission

Student Profile
  - View/edit profile with tabs: Personal, Academic, Contact, Documents, Transfer Certificate
  - Photo key field for profile photo storage
  - Blood group, religion, nationality, category (General/OBC/SC/ST/EWS)

Document Management
  - Upload documents per student (birth certificate, marksheet, etc.)
  - Delete documents
  - File name and key stored for cloud retrieval

Student Roster
  - Paginated student list with search
  - Click row to open student profile
  - Filter by classroom

Bulk Class Promotion
  - 3-step wizard: select from/to classroom + academic year → select students → confirm
  - Shows success count after promotion

Transfer Certificates
  - Issue TC with auto-generated number: TC/{SLUG}/{YEAR}/{SEQ:0004d}
  - Record reason and remarks
  - List all TCs per school

Parent Management
  - Link parents to students
  - Pending parent invite (phone + name) for app onboarding
  - Multiple children support via StudentParentLink

──────────────────────────────────────────────────────────
MODULE C — ACADEMIC OPERATIONS

Timetable Management
  - Create timetable per classroom per academic year
  - Week grid builder: Monday–Friday columns, period rows
  - Slot types: lesson, break, free
  - Assign subject and teacher per slot
  - Bulk slot save (atomic replace)

Homework
  - Assign homework with title, description, subject, classroom, due date
  - Filter by classroom and subject
  - Overdue indicator (color-coded by due date)
  - Edit and delete assignments

Exam Management
  - Create exams with type (unit test / midterm / final / other), date range
  - Publish/unpublish exams
  - Add per-subject per-classroom exam schedules (date, time, max marks, passing marks)
  - 3-panel drill-down: exam list → schedules → marks entry

Marks Entry
  - Bulk marks entry per exam schedule
  - Per-student marks input with absent checkbox
  - Auto-computed grade (A+/A/B/C/D/F) saved on the server
  - Upsert — re-entry corrects existing marks

Report Cards
  - Generate report cards for a classroom + exam combination
  - Auto-computed: total marks, obtained marks, percentage, grade, rank
  - Dense ranking algorithm (ties share rank)
  - Publish report cards to parents
  - Summary stats: class average, top score, pass rate
  - Grade color badges (A+=green, A=teal, B=blue, C=yellow, D/F=red)

──────────────────────────────────────────────────────────
TEACHERS

Teacher Roster
  - List all teachers with Active / On Leave status badges
  - Class teacher assignment shown per teacher
  - Click row to view full profile

Teacher Profile
  - Assigned classroom (class teacher role)
  - Weekly schedule grid (Mon–Fri, periods by subject and time)
  - Leave history with approve / reject actions
  - Substitution history tab

Leave Management
  - Apply leave: type (sick / casual / other), date range, reason
  - Admin can approve or reject pending leaves
  - Leave status: pending → approved / rejected

Substitute Teacher Assignment
  - Action items banner: shows all uncovered timetable slots for teachers on approved leave
  - Per-slot substitute selection dropdown (choose from other teachers)
  - Assign substitute — recorded against the specific leave + date + slot
  - Action items clear once all slots for a leave are covered

──────────────────────────────────────────────────────────
UDISE+ (Government Data Submission)

Annual UDISE Report
  - One report per school per academic year
  - UDISE+ school code field (12-digit)
  - Draft / Submitted status with submission timestamp

Section 1 — Students (Auto-filled)
  - Enrollment table by class: total, boys, girls
  - Category breakdown: SC, ST, OBC, EWS, General
  - Grand totals row

Section 2 — Teachers (Auto-filled)
  - Full teacher list: name, email, employee code, class teacher assignment
  - Total teacher count summary card

Section 3 — Infrastructure (Manual Entry, ~25 fields)
  - School Identity: type, management, year established, class range
  - Physical Infrastructure: classroom counts by condition, other rooms, boundary wall, playground
  - Toilets & Sanitation: boys/girls/staff/CWSN toilets, ramp, handwashing
  - Utilities & Connectivity: electricity, solar, internet, drinking water source, rainwater harvesting
  - Resources & Facilities: computers, library, books, mid-day meal, kitchen garden, medical checkup

Section 4 — Academics (Auto-filled)
  - Exam-wise summary from published report cards
  - Columns: exam name, type, year, students, passed, pass %, average %
  - Pass rate color-coded (green ≥75%, amber ≥50%, red <50%)
  - Section nav with completion indicators (✓ complete / ⚠ needs input)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — NAVIGATION & SHELL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Sidebar Navigation (Permission-Aware)
  - Overview: Dashboard
  - Academics: Students, Teachers, Timetable, Homework, Exams, Report Cards
  - School Setup: Academic Years, Subjects, Houses, Calendar, UDISE+
  - Transport: Live Fleet, Car Pickup, Routes, Drivers, Vehicles
  - Administration: Staff, Roles & Permissions
  - Finance: Payments
  - Operations: Call Mgmt, Communications, Compliance, Reports
  - Settings: Settings
  - Grouped navigation with labeled sections
  - Groups with no accessible items hidden entirely

Top Bar
  - Global search input
  - Notifications panel
  - Settings icon

School Identity Block
  - School name and logo in sidebar
  - Current academic year display
  - Logged-in user info with role and logout button

Dashboard Reports
  - Attendance reports: daily/weekly, CSV download
  - Fleet reports: trips, routes
  - Alert log: panic events, broadcasts

Communications
  - School-wide broadcast messages
  - Per-classroom broadcast from teacher app

Compliance
  - Compliance tracking module

Payments / Billing
  - School subscription billing via Razorpay
  - Subscription plan management

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — OPERATIONS PAGES (Built)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Call Management (/calls) — pre-existing, already fully implemented
  - Inbound parent call request queue: Approve & queue, Decline with optional note
  - AI mass-call campaign builder: name, objective, target audience, script context, rate limit
  - Campaign lifecycle: Draft → Approved → Queued → Running → Paused / Completed
  - Progress bar per campaign with calls completed/failed counts
  - Call token gauge with Low/Critical alerts and "get more tokens" CTA
  - Recent activity sidebar
  - Stat cards: Pending requests, Token balance, Active campaigns, Completed calls

Communications (/communications)
  - School-wide broadcast composer: title, message body (160-char limit), audience, channel
  - Audience options: All parents, Classroom, Bus route, Custom list
  - Channel options: Push notification only, SMS only, Push + SMS
  - Character count indicator in composer
  - Broadcast history cards with delivery rate progress bar and percentage
  - Stat cards: Total broadcasts, Parents reached, Average delivery rate

Compliance (/compliance)
  - Renewal reminder list from GET /api/compliance/driver/renewals/
  - Urgency tiers: Overdue (red), Urgent ≤7 days (amber), Due soon ≤30 days (blue), OK (green)
  - Sorted by nearest expiry date
  - "Reminder sent" badge per item when automated reminder has fired
  - Stat cards: Total items, Overdue, Urgent, All OK
  - Filter tabs: All / Overdue / Urgent / Due soon

Reports (/reports)
  - School overview stat cards: Students, Teachers, Drivers, Published exams
  - This week's attendance bar chart (animated, color-coded by attendance %)
  - Export cards for 6 report types: Student Roster, Teacher Directory, Trip Summary, Exam Results, Attendance Log, Report Cards (CSV export via backend)
  - "More analytics coming soon" section for future roadmap features

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — ADMINISTRATION PAGES (Built)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Staff (/staff)
  - Non-teaching staff roster with expandable detail rows (phone, email, joined date)
  - Status badges: Active / On Leave / Inactive
  - Add/edit drawer: name, role (Librarian/Accountant/Clerk etc.), department, employee code, phone, email, status
  - Filter tabs: All / Active / On Leave; search by name or role
  - Stat cards: Total, Active, On Leave

Roles & Permissions (/roles)
  - System roles (read-only) and custom roles shown in separate sections
  - Expandable permission badge list per role (e.g. "Manage Students", "Enter Marks")
  - System roles cannot be edited or deleted (locked)
  - Create/edit role drawer with permission checkboxes grouped into 7 categories:
    Students, Staff, Finance, Attendance, Academics & Exams, Communications & Reports, Settings
  - Group-level toggle (select/deselect all in a category at once)
  - "Select all / Deselect all" global toggle
  - Stat cards: Total roles, Custom roles, Permissions available
  - Superuser wildcard ["*"] shown as "all access" in expanded view

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — TRANSPORT PAGES (Built)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Live Fleet (/live-fleet)
  - List of all trips from GET /api/tracking/fleet/live/
  - Trip cards: route name, bus registration, driver, shift, boarded/total progress bar
  - Status badge: Active (green) / Scheduled (blue) / Completed / Cancelled
  - Live GPS coordinates shown for active trips; "No GPS data" shown when unavailable
  - Green pulse dot on active trip bus icon
  - Auto-refresh every 30 seconds; manual Refresh button
  - Stat cards: Total trips, Active now, Scheduled
  - Info banner explaining GPS transmission behavior

Routes (/routes)
  - Route cards with expandable stop sequence visualization
  - Stop sequence shown as a numbered vertical timeline with connector lines
  - Add/edit route drawer: name, vehicle assignment (dropdown), stop list builder
  - Stop list: add/remove stops inline, numbered with drag handle indicators
  - Delete route with confirmation
  - Stat cards: Total routes, With vehicle, Total stops
  - Shows warning when no vehicle is assigned to a route

Drivers (/drivers)
  - Driver rows with expandable detail panel (Aadhaar, added date, vehicle, route)
  - Status badge: Active (approved) / Pending (not approved)
  - Pending signup requests shown as a distinct section with Approve/Reject buttons
  - Add driver drawer: name, phone, optional Aadhaar → generates invite token on save
  - Invite token shown in drawer after creation with copy button
  - SMS automatically sent to driver's phone with invite code (via backend)
  - Edit driver: update name and phone
  - Delete driver with confirmation
  - Search by name or phone number
  - Stat cards: Total, Active, Pending review
  - Cross-action: route and vehicle info sourced from DriverVehicleAssignment

Vehicles (/vehicles)
  - Vehicle cards: registration number (monospace), type badge, seat count, route and driver info
  - Type badges color-coded: Bus (blue), Minibus (purple), Van (teal), Tempo (amber), Other (slate)
  - Warning shown when vehicle has no route assigned
  - Add/edit vehicle drawer: registration number (auto-uppercased), vehicle type pill selector, capacity
  - Delete vehicle with confirmation
  - Stat cards: Total vehicles, On a route, Total seats

Car Pickup (/dismissal) — pre-existing, already implemented

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — ACADEMICS PAGES (Built)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Classrooms (/classrooms)
  - Grid of classroom cards grouped by class name (e.g., Class 6, Class 7)
  - Color-coded accent per class: nursery=pink, primary=blue, middle=purple, secondary=teal, senior=red
  - Each card shows: classroom name + section, assigned class teacher name, student count
  - Stat cards: Total Classrooms, Total Students across all rooms, Class Teachers assigned vs unassigned
  - Add/Edit classroom drawer (right-slide, spring animation):
      - Cascading dropdowns: first pick class name (Nursery/LKG/UKG/Class 1–12 or custom), then section (A/B/C/D/E or custom) appears
      - Live preview badge updates as name/section are selected
      - Optional class teacher assignment (select from teachers list)
  - "Add section" ghost button inside each group for quick sibling-section creation
  - Delete classroom with confirmation modal showing affected student count
  - Backend: GET/POST /api/academics/admin/classrooms/, PATCH/DELETE /api/academics/admin/classrooms/{id}/

Teachers (/teachers) — Enhanced
  - Classroom assignment section in each expanded teacher row:
      - Cascading dropdowns: select class name then section (sourced from classrooms list)
      - "Assign Classroom" button PATCHes the classroom's teacher_id
      - Green "Assigned!" confirmation flash on success
  - AI Substitute Finder button on any teacher row with leave_status = "on_leave":
      - Opens AutoSubstitutePanel (right-slide drawer)
      - Shows absent teacher info and active leave details
      - Cascading classroom selector to identify the vacant class
      - "Find Best Substitute" calls POST /api/academics/admin/substitute/suggest/
      - Displays AI recommendation card with gradient: teacher name, subjects, one-sentence reasoning
      - Lists all available teachers (not on leave today) ranked by suitability
      - "Recommended" tag highlights the AI pick among the full list

Timetable (/academics/timetable)
  - Cascading class selector: first pick class name, then section dropdown appears (filtered from classrooms list)
  - Class + academic year selector to load the right timetable
  - Week grid: Mon–Fri (or Mon–Sat toggle) columns × period rows
  - 8 default periods from 08:00–15:00; each row shows editable start/end time
  - Click any cell → popover: slot type (Lesson / Break / Free), subject dropdown, teacher dropdown
  - Slot types color-coded: Lesson (blue), Break (amber), Free (slate)
  - Add / delete period rows from the header column
  - "Save timetable" bulk-replaces all slots atomically via POST
  - Auto-creates timetable record if one doesn't exist for the classroom + year pair
  - Dirty indicator: unsaved changes badge on the save button
  - Cross-action: teacher dropdown populated from the teachers roster

Homework (/academics/homework)
  - Card list of all homework assignments with subject color accent border
  - Due date badge: Overdue (red), Due today (amber), Upcoming (green)
  - Search across title + description; filter by classroom and subject
  - Status tabs: All / Active / Inactive
  - Stat cards: Total, Active, Overdue count
  - Add/edit drawer: classroom, subject, title, description, assigned date, due date
  - Delete with confirmation dialog
  - Cross-action: subject and classroom dropdowns sourced from live data

Exams (/academics/exams)
  - Exam list with type badge (Unit Test / Midterm / Final / Practical / Other) and date range
  - Publish / Unpublish toggle per exam (POST …/publish/)
  - Stat cards: Total, Published, Draft
  - 3-level drill-down: Exam → Schedules panel → Marks entry slide panel
  - Schedules panel: add subject + classroom + date + time + max/passing marks
  - Marks panel (MarksPanel): full student list for the classroom with marks input + absent checkbox
  - Real-time grade preview (A+/A/B/C/D/F) computed client-side as marks are typed
  - Bulk marks save (upsert) via POST …/results/
  - Cross-action: subject and classroom dropdowns from live data; teacher assignment from teachers module

Report Cards (/academics/report-cards)
  - Exam + classroom selector to scope the report
  - "Generate" button → POST …/report-cards/generate/ (regenerates on every run)
  - Auto-loads existing report cards when classroom is changed
  - Ranked table: 🥇🥈🥉 medals for top 3; suffix ranks (4th, 5th…) for rest
  - Per-row: student name, admission number, marks (obtained/total), percentage progress bar, grade badge
  - Grade badge colors: A+=green, A=teal, B=blue, C=amber, D=orange, F=red
  - Dense ranking footnote reminder
  - Grade distribution breakdown row (A+ N (%), A N (%), …)
  - Summary stat cards: Students, Class average %, Top score %, Pass rate (with pass count)
  - Pass rate color: green ≥75%, amber ≥50%, red <50%
  - "Publish to parents" button → POST …/report-cards/publish/ (marks all is_published=true)
  - Published status shown per-row (sent checkmark) and as a "Published" pill replacing the publish button
  - Empty state with direct Generate CTA when no cards exist for selection

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADMIN DASHBOARD — SCHOOL SETUP PAGES (Built)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Academic Years Dashboard (/academics/years)
  - List of all academic years with date ranges and session counts
  - Current year highlighted with a primary-color border
  - "Set as current" button on any non-current year
  - Expandable sessions panel per year with inline session management
  - Add sessions inline (name + date range) without leaving the page
  - Set a session as current from the inline panel
  - Edit year drawer: name, start/end dates, current flag
  - Create / delete academic years
  - Stat cards: total years, current year name, total sessions

Subjects Dashboard (/academics/subjects)
  - Color-coded subject cards in a responsive grid
  - Color deterministically computed from subject name (no manual selection)
  - Active/inactive toggle per subject card (persisted to backend)
  - Add/edit subject drawer: name + optional subject code
  - Color preview bar animates in the drawer as name is typed
  - Filter by All / Active / Inactive
  - Search by name or code
  - Stat cards: total, active, inactive

Houses Dashboard (/academics/houses)
  - Visual house cards with colored header band and house icon
  - Color picker: 8 preset swatches + full custom color input
  - Live preview of house name and color in the drawer
  - Create / edit / delete houses with description field
  - "Add house" ghost card inline in the grid for quick creation
  - Info banner: reminder to create houses before student admission

Academic Calendar (/academics/calendar)
  - Year selector synced to the current academic year
  - Two tabs: Holidays and Events with live counts
  - Stat row per tab showing counts by type (national/regional/school or exam/sports/cultural/parent_meeting/other)
  - Holidays: name, date, type badge, edit/delete
  - Events: title, date range, type badge, description, edit/delete
  - Add Holiday drawer: name, date, type selector (pill buttons)
  - Add Event drawer: type selector, title, start/end dates, description
  - All data filtered by selected academic year

UDISE+ Dashboard (/udise)
  - List of annual UDISE reports per school
  - Each report card shows: academic year, UDISE code, Draft/Submitted badge, section status icons
  - Section guide card explaining what's auto-filled vs manual
  - Create report drawer: year selector + 12-digit UDISE code
  - Deduplication: creating a report for an existing year opens the existing one
  - Section status icons: Students (auto), Teachers (auto), Infrastructure (manual), Academics (auto)
  - Submitted reports shown with green border and FileCheck icon

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INTEGRATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  - Firebase FCM         — Push notifications to parents and conductors (boarded, dropped, SOS)
  - MSG91                — SMS fallback for parents without internet
  - Google Maps          — Live tracking map + route builder (parent app + dashboard)
  - Razorpay             — School subscription payments and billing
  - Cloudflare R2 / AWS S3 — Student photos, KYC documents, file storage
  - Google Gemini (AI)   — AI Teaching Assistant (lesson plans, class summaries, voice observations, auto-substitute suggestions)
  - DigiLocker API       — Conductor license and ID verification (Phase 2)
  - VAHAN REST API       — Vehicle registration verification (Phase 2)
  - Django Channels + Redis — WebSocket real-time GPS and fleet updates
  - Celery + Redis       — Async notifications, background tasks

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INFRASTRUCTURE & DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  - Backend: Django 5 + DRF + Daphne (ASGI) on port 8000
  - Marketing Website: Next.js 14 + Tailwind on port 3001
  - Admin Dashboard: Next.js 14 on port 3000
  - Parent App: Expo (React Native Web) on port 8081
  - Driver App: Expo (React Native Web) on port 8082
  - Teacher App: Expo (React Native Web) on port 8083
  - PostgreSQL 16 on port 5432
  - Redis 7 on port 6379
  - Celery worker for async tasks
  - Docker Compose with named profiles: backend, frontend, mobile
  - Health check endpoint: GET /health/ → {"status":"ok"}
  - OpenAPI schema at /api/schema/ (drf-spectacular)
  - Django Admin at /admin/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MODULES PLANNED (Not Yet Built)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Module D — Staff Management
  - Full staff profiles (non-teaching staff)
  - Teacher / staff attendance tracking
  - Leave requests and approvals (for non-teaching staff)
  - Basic payroll

Module E — Finance (Enhanced)
  - Receipt generation for fee payments
  - Due date reminders
  - Fine management

Module F — Reports (Enhanced)
  - Detailed attendance reports
  - Student progress reports
  - Fee collection reports
  - Staff activity reports

Phase 2 Integrations
  - DigiLocker: Government-backed conductor license and ID verification
  - VAHAN: Vehicle registration verification

Mobile App Enhancements
  - Play Store deployment (Parent App + Conductor App)
  - In-app conductor-to-parent messaging (Chat screen)
  - Face verification accuracy improvements
  - Battery optimization tuning for background GPS
